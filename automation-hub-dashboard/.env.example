# Backend API base URL for the live dashboard (FastAPI Automation Hub).
VITE_API_BASE=http://localhost:8000
# Dev webhook/control secret (matches HUB_WEBHOOK_SECRET on the backend).
VITE_WEBHOOK_SECRET=dev-webhook-secret
